@model meivorts.Models.TipoCompromisso

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <p>
        <button class="glyphicon glyphicon-backward btn-sm btn btn-primary" type="button" onclick="location.href='@Url.Action("")'"> Voltar</button>
    </p>
    <form class="form-horizontal" role="form">
        <div class="form-group">
            <fieldset>
                <legend>Tipo Compromisso</legend>
                <div class="control-group">
                    <div class="control-label">
                        @Html.LabelFor(model => model.NomeCompromisso)
                    </div>
                    <div class="control">
                        @Html.TextBoxFor(model => model.NomeCompromisso, new { @class = "form-control", @style = "Width:20%" })
                        @Html.ValidationMessageFor(model => model.NomeCompromisso)
                        <p></p>
                    </div>
                </div>
                <div class="control-group">
                    <div class="controls">
                        <button class="btn btn-success" type="submit">Salvar</button>
                    </div>
                </div>
            </fieldset>
        </div>
    </form>
}